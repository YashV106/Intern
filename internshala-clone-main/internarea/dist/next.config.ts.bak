import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,

  // Produce a server bundle we can run on Netlify (instead of static export only).
  output: "standalone",
};

export default nextConfig;
